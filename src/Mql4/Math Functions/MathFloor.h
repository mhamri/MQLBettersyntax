/**
 * @brief The function returns integer numeric value closest from below.
 *
 * @param val [in]  Numeric value.
 * @return double A numeric value representing the largest integer that is less than or equal to val.
 *
 * @note Instead of MathFloor() you can use floor().
 */
double MathFloor(
    double val  // number
);

/**
 * @brief The function returns integer numeric value closest from below.
 *
 * @param val [in]  Numeric value.
 * @return double A numeric value representing the largest integer that is less than or equal to val.
 *
 * @note Instead of MathFloor() you can use floor().
 */
double floor(
    double val  // number
);