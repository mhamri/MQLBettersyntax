/**
 * @brief The function returns the arccosine of x within the range 0 to π in radians.
 *
 * @param val [in]  The val value between -1 and 1, the arc cosine of which is to be calculated.
 * @return double Arc cosine of a number in radians. If val is less than -1 or more than 1, the function returns NaN (indeterminate value).
 *
 * @note Instead of the MathArccos() function you can use acos().
 */
double MathArccos(
    double val  // -1<val<1
);

/**
 * @brief The function returns the arccosine of x within the range 0 to π in radians.
 *
 * @param val [in]  The val value between -1 and 1, the arc cosine of which is to be calculated.
 * @return double Arc cosine of a number in radians. If val is less than -1 or more than 1, the function returns NaN (indeterminate value).
 *
 * @note Instead of the MathArccos() function you can use acos().
 */
double acos(
    double val  // -1<val<1
);