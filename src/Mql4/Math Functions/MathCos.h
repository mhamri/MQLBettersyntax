/**
 * @brief The function returns the cosine of an angle.
 *
 * @param value [in]  Angle in radians.
 * @return double Value of double type within the range of -1 to 1.
 *
 * @note Instead of MathCos() you can use cos().
 */
double MathCos(
    double value  // number
);

/**
 * @brief The function returns the cosine of an angle.
 *
 * @param value [in]  Angle in radians.
 * @return double Value of double type within the range of -1 to 1.
 *
 * @note Instead of MathCos() you can use cos().
 */
double cos(
    double value  // number
);