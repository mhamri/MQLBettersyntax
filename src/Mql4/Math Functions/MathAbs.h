/**
 * @brief The function returns the absolute value (modulus) of the specified numeric value.
 * 
 * @param value [in]  Numeric value.
 * @return double Value of double type more than or equal to zero.
 * 
 * @note Instead the MathAbs() function you can use @ref fabs().
 */
double MathAbs(
    double value // numeric value
);