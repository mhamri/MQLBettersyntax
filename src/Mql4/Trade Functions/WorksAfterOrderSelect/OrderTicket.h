/**
 * @brief Returns ticket number of the currently selected order.
 *
 * @return int Ticket number of the currently selected order.
 * @note The order must be previously selected by the OrderSelect() function.
 */
int OrderTicket();