#include <iostream>
using namespace std;
/**
 * @brief The symbol name of the currently selected order.
 *
 * @return string The symbol name of the currently selected order.
 * @note The order must be previously selected by the OrderSelect() function.
 */
string OrderSymbol();