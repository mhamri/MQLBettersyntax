#include <iostream>
using namespace std;

/**
 * @brief Returns comment of the currently selected order.
 *
 * @return string Comment of the currently selected order.
 * @note The order must be previously selected by the OrderSelect() function.
 */
string OrderComment();