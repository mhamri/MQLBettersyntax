/**
 * @brief Returns calculated commission of the currently selected order.
 *
 * @return double The calculated commission of the currently selected order.
 * @note The order must be previously selected by the OrderSelect() function.
 */
double OrderCommission();