/**
 * @brief Returns amount of lots of the selected order.
 *
 * @return double Amount of lots (trade volume) of the selected order.
 * @note The order must be previously selected by the OrderSelect() function.
 */
double OrderLots();