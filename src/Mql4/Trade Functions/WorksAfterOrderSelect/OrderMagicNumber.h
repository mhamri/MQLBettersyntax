/**
 * @brief Returns an identifying (magic) number of the currently selected order.
 *
 * @return int The identifying (magic) number of the currently selected order.
 * @note The order must be previously selected by the OrderSelect() function.
 */
int OrderMagicNumber();