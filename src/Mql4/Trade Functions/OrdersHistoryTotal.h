/**
 * @brief Returns the number of closed orders in the account history loaded into the terminal.
 *
 * @return int The number of closed orders in the account history loaded into the terminal. The history list size depends on the current settings of the "Account history" tab of the terminal.
 */
int OrdersHistoryTotal();