#include <iostream>
using namespace std;

/**
 * @brief Returns the name of a symbol of the current chart.
 * 
 * @return string Value of the _Symbol system variable, which stores the name of the current chart symbol.
 */
string Symbol();