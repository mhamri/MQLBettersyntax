enum ENUM_OP_OrderProperties
{
    // Buy operation
    OP_BUY = 0,
    // Sell operation
    OP_SELL = 1,
    // Buy limit pending order
    OP_BUYLIMIT = 2,
    // Sell limit pending order
    OP_SELLLIMIT = 3,
    // Buy stop pending order
    OP_BUYSTOP = 4,
    // Sell stop pending order
    OP_SELLSTOP = 5,
};
