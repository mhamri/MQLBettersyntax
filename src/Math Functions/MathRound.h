/**
 * @brief The function returns a value rounded off to the nearest integer of the specified numeric value.
 * 
 * @param value [in]  Numeric value before rounding.
 * @return double Value rounded till to the nearest integer.
 * 
 * @note Instead of MathRound() you can use @ref round().
 */
double MathRound(
    double value // value to be rounded
);