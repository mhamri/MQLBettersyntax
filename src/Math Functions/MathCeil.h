/**
 * @brief The function returns integer numeric value closest from above.
 * 
 * @param val [in]  Numeric value.
 * @return double Numeric value representing the smallest integer that exceeds or equals to val.
 * 
 * @note Instead of the MathCeil() function you can use ceil().
 */
double MathCeil(
    double val // number
);

/**
 * @brief The function returns integer numeric value closest from above.
 * 
 * @param val [in]  Numeric value.
 * @return double Numeric value representing the smallest integer that exceeds or equals to val.
 * 
 * @note Instead of the ceil() function you can use MathCeil().
 */
double ceil(
    double val // number
);