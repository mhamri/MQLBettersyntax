


/** @brief The function returns the real remainder of division of two numbers.
 * @param value [in]  Dividend value.
 * @param value [in]  Divisor value.
 * @return The MathMod function calculates the real remainder f from expression val/y so that val = i * y + f , where i is an integer, f has the same sign as val, and the absolute value of f is less than the absolute value of y.

*/
double  MathMod( 
   double  value,      // dividend value 
   double  value2      // divisor value 
   );
